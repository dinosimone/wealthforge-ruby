require "spec_helper"

describe WealthForge do
  it "has a version number" do
    expect(WealthForge::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
